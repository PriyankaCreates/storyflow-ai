{
  "name": "deterministic-story-pipeline",
  "version": "0.1.0",
  "private": true,
  "description": "Research prototype for generating narrated multi-scene story videos.",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js"
  },
  "engines": {
    "node": ">=20"
  }
}

